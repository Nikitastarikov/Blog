<p id="notice"><%= notice %></p>

<h1>Posts</h1>
	<% if user_signed_in? && current_user.admin? %>
		<%= link_to 'Новая статья', new_admin_post_path, class: 'btn btn-outline-primary pull-sm' %>
	<% end %>
<section id='posts'>
	<%= render @posts %>
</section>


